RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route API requests to api.php
RewriteRule ^api(/.*)?$ api.php [QSA,L]

# Route other requests to index.php (MVC)
RewriteRule ^(.*)$ index.php [QSA,L]
